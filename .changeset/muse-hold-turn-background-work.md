---
"@kontourai/station-contracts": minor
---

Add `MUSE_LINGERING_CHILD_REAPED_CODE`, the `runtime.warning` code a Muse turn publishes when Station's idle deadline stops a child that may still be running background work its already-ended turn launched.
