{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "desktop-updater",
  "description": "Self-update check and install/restart, scoped to the platforms tauri-plugin-updater and tauri-plugin-process are dependencies on. A shared capability file referencing these would fail mobile ACL resolution: the plugins are not compiled there at all (station#575).",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "updater:allow-check",
    "updater:allow-download-and-install",
    "process:allow-restart"
  ]
}
