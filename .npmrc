# pnpm installation settings live in pnpm-workspace.yaml.
# These defaults also protect retained npm script/publication tooling.
registry=https://registry.npmjs.org
engine-strict=true
# All dependency lifecycle code starts disabled. The checked-in lifecycle
# runner is the only path that may enable a reviewed package build.
ignore-scripts=true
