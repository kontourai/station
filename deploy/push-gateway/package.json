{
  "name": "station-push-gateway",
  "private": true,
  "type": "module",
  "description": "Forwards Station-signed agent-activity pushes to FCM. Deployed by Kontour; not part of the Station workspace.",
  "scripts": {
    "test": "cd ../.. && npm run test:focused -- deploy/push-gateway/test/*.test.ts",
    "deploy": "npx --yes wrangler@4 deploy"
  }
}
