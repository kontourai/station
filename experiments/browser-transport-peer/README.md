# Pion peer source moved

The qualified Pion application peer is production-owned under [`src-server/services/connections/pion-peer`](../../src-server/services/connections/pion-peer/README.md). Production code never imports or executes content from `experiments/`.
