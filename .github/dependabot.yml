version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      # Grouped bumps must stay reviewable and landable. A group that mixes
      # majors with patches makes a routine patch wait on a compiler
      # migration: #11 and #953 both bundled TypeScript 5 -> 7, Vite 7 -> 8
      # and plugin-react 5 -> 6 with two vitest patches, so the whole group
      # sat red while vitest 4.1.11 (which was fine) could not land until it
      # was split out by hand (#950). Majors are excluded here so each
      # arrives as its own PR and can get the canary it needs (#918).
      frontend-tooling:
        update-types:
          - "minor"
          - "patch"
        patterns:
          - "@vitejs/*"
          - "vite"
          - "vitest"
          - "@vitest/*"
          - "@playwright/*"
          - "typescript"
          - "tsx"
      observability:
        patterns:
          - "@opentelemetry/*"
      aws:
        patterns:
          - "@aws-sdk/*"
          - "@smithy/*"
    # esbuild is exact-pinned by @kontourai/flow-agents (station#1719), so a
    # root-only bump splits the shared node_modules/esbuild copy into
    # separate, unhoisted nested copies and cannot pass the
    # dependency-lifecycle allowlist gate without a human review anyway.
    # Ignored until #1719 resolves upstream; re-enable there.
    ignore:
      - dependency-name: "esbuild"
  - package-ecosystem: cargo
    directory: /src-desktop
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
