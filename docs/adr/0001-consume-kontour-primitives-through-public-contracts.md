# Consume Kontour primitives through public contracts

Station consumes Surface, Flow, Veritas, Survey, Flow Agents, Console, and Kontour UI only through their published contracts: packages, CLIs, MCP servers, file formats, or documented HTTP surfaces. This keeps Station a consumer rather than the authority over those primitives, and prevents private sibling-repo imports from becoming hidden product semantics.
