// Template for a conformant route view: it renders its content and no page
// header at all — `PageFrame` renders the header for the route, above it.
export function __COMPONENT__() {
  return (
    <div className="scratch-view">
      <h3>Section</h3>
    </div>
  );
}
