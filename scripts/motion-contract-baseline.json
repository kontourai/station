{
  "hardCodedDeclarationCeiling": 1,
  "perFileCeilings": {
    "src-ui/src/components/notifications/NotificationHistory.css": 1
  },
  "recordedAt": "2026-08-29T00:00:00Z",
  "recordedBy": "station#753 motion polish pass, item 6: migrated the remaining 25 files' 99 literal declarations to semantic tokens (a duration/easing value that had no exact token used the nearest token by ROLE — e.g. a `spin` keyframe always takes `--motion-status-spin` regardless of its original number, a recurring `cubic-bezier(0.16, 1, 0.3, 1)` settle curve took `--ease-emphasized`, `cubic-bezier(0.4, 0, 0.2, 1)` took `--ease-standard` as an exact match; MonitoringWidgets.css's `slideInFade` keyframe's dead 95%-hold segment was deleted rather than tokenized, per the 'or delete dead rules' allowance). One declaration is left on a ceiling deliberately: NotificationHistory.css's `notification-dismiss-collapse` animation duration is `4s` because it visually encodes `UNDO_WINDOW_MS = 4000` in NotificationHistory.tsx — an actual coupled dismiss-window timing, not a motion-grammar duration, and no token can express it without breaking that correspondence. Global ceiling 100->1, every other per-file entry removed."
}
