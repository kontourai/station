{
  "hardCodedDeclarationCeiling": 3,
  "perFileCeilings": {
    "src-ui/src/components/notifications/NotificationHistory.css": 1,
    "src-ui/src/components/voice/VoicePill.css": 2
  },
  "recordedAt": "2026-08-29T00:00:00Z",
  "recordedBy": "station#753 motion polish pass, item 6: migrated the remaining 25 files' 99 literal declarations to semantic tokens (a duration/easing value that had no exact token used the nearest token by ROLE — e.g. a `spin` keyframe always takes `--motion-status-spin` regardless of its original number, a recurring `cubic-bezier(0.16, 1, 0.3, 1)` settle curve took `--ease-emphasized`, `cubic-bezier(0.4, 0, 0.2, 1)` took `--ease-standard` as an exact match; MonitoringWidgets.css's `slideInFade` keyframe's dead 95%-hold segment was deleted rather than tokenized, per the 'or delete dead rules' allowance). Two declarations are left on ceilings deliberately, both coupled timing constants a token would decouple rather than express: NotificationHistory.css's `notification-dismiss-collapse` animation duration is `4s` because it visually encodes `UNDO_WINDOW_MS = 4000` in NotificationHistory.tsx; VoicePill.css's two `0.08s` transitions (`.voice-pill--listening`, `.voice-pill__ring`) smooth `--audio-scale`/`--audio-glow`, republished on every microphone frame (NovaVoiceSessionAdapter.ts `handleMicrophoneFrame`) — Direct-manipulation category, not Feedback/state; each has a local test pinning its literal (VoicePill.motion-exception.test.ts, NotificationHistory tests) per motion.md's exception-needs-a-local-test rule. Fix-round correction: the review's own count for this round (\"global ceiling 1->2\") undercounted — VoicePill.css contributes TWO declarations (its listening-state transition and its ring's transition are separate `transition:` blocks), not one, so the correct global ceiling is 1 (prior) + 2 (VoicePill) = 3, verified by running the ratchet script directly rather than counting by hand. Global ceiling 100->1 (station#753 main pass), 1->3 (this fix round)."
}
