{
  "_note": "repo-guardrails remains advisory: it currently passes with 104 baselined violations, and no npm-run lane root or workflow run block invokes \"npm run proof:repo-guardrails\" (station#2299). It is not unreached, though: the test file named in _corpusExecution runs the proof inside the Vitest corpus and asserts its exit status, including a positive control that the unmutated proof passes; scripts/vitest-resource-manifest.mjs classifies that file as process-heavy, so full:regression -- the nightly and release gate -- is the lane that runs it. The acknowledgement names its executor so the label stays derived from a file the gate checks rather than from prose (#1746). Promoting the check to enforced is a separate decision that also depends on the station#2765 burn-down.",
  "_corpusExecution": {
    "repo-guardrails": "scripts/__tests__/proof-repo-guardrails-fail-closed.test.ts"
  },
  "repo-governance": "enforced",
  "verification-policy": "enforced",
  "repo-guardrails": "advisory",
  "architecture-boundaries": "candidate",
  "ui-data-access": "candidate",
  "runtime-contracts": "candidate",
  "retired-surfaces": "candidate",
  "migration-tombstones": "candidate",
  "connected-agents": "enforced",
  "static-verification": "enforced",
  "sdk-builds": "enforced",
  "app-builds": "enforced",
  "fallow-advisory": "advisory"
}
