{
  "bespokeHeaderCeiling": 0,
  "stackedHeadingCeiling": 0,
  "recordedAt": "2026-08-21T00:00:00Z",
  "recordedBy": "station UX audit lane C1 (SHELL-11/SHELL-17), round 2. `bespokeHeaderCeiling` counts, over a GLOB of every git-tracked .tsx under src-ui/src/views/** and src-ui/src/pages/**, files that write a page header themselves: canonical class markup (page__header/page__title/page__label), a page-level <h1>, or a HEADER BLOCK carrying an <h1>/<h2> \u2014 an element whose class names a *__header/*-header family, or a <header> element. The third signal is round 2's: the first two only recognised a bespoke header written in the canonical vocabulary, so ordinary markup in a vocabulary of its own (<header className=\"tools-view__header\"><h2>Tools</h2></header>) passed at ceiling zero \u2014 the exact shape that produced six title sizes and nine title x-positions across 28 routes. <h3> is deliberately not counted (the level the shell rule prescribes for card, dialog and section headers; including it matched 16 files here, an exception list that long being a rubber stamp). The canonical-class guard now exempts exactly `project-page__header` rather than every `*-page__header`. Recorded at 0 with five exceptions named in the script with their reasons: Home's hero prompt, the out-of-shell shared-answer route, the project workspace's identity row, and two dialog surfaces whose <h2> is a modal's accessible name rather than a heading for a route. stackedHeadingCeiling is unchanged at 0 and its signal is byte-identical."
}
