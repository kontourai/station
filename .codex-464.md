Role: Codex engineering delegate
Mode: implement
Repo/worktree: the directory you were launched in — standalone clone of kontourai/station, branch fix/189-usage-truth cut from origin/main (9827a88d5ec4). Work ONLY here.
Goal: fix all four parts of issue #464 (usage accounting hardening, epic #189). You have NO network — never run gh or fetch.

Environment (verified): prefix npm/npx with PATH="$(mise where node@24)/bin:$PATH". Deps installed, connect+cli dist built. Unit tests: npm run test:focused -- <files>. Lint: npm run lint:check. Types: npm run typecheck. Gate routing: npm run gate:for -- <paths>. Read AGENTS.md at root and src-server/AGENTS.md before editing. Never touch .kontourai/.veritas/.flow; stop-hook noise about other lanes is EXTERNAL. No git stash/reset/clean. No push/gh/publish. Write nothing outside this worktree and /tmp. Don't disturb other processes (sibling sessions run tests here). COMMIT EARLY AND OFTEN — one commit per part; prior sessions on this host have died mid-run and only committed work survives. Append "Co-authored-by: codex gpt-5.6-sol" to each commit body.

A fresh read-only verification pinned ALL FOUR parts as still-present on 9827a88d5ec4, with exact locations. Fix them in THIS ORDER (part b first — it is the only one that currently 500s a user-facing endpoint):

**(b) NaN-capable Claude birth site → 500 on the stats endpoint.** `src-server/providers/adapters/claude-adapter-events.ts:483-485` reads `message.usage.input_tokens` / `output_tokens` UNGUARDED and sums them, while the SAME file already has the guard (`tokenCount`, :19-24) and applies it to the cache fields (:38-39), context occupancy (:59-61) and cost (:510-514, whose comment says a crashed/startup-error result can carry missing/non-numeric values). A NaN flows through `usage-fold.ts:472-486` (which accumulates promptTokens with no Number.isFinite check, unlike `isUsableCost` :393-395) into `conversation-manager.ts:167-177`, whose `reportedTokenFigureIsBroken` THROWS, which `conversations.ts:2211-2217` turns into a 500 for that conversation's stats.
Fix: apply the existing `tokenCount` guard at the birth site (respecting absent≠zero — an absent field must stay absent, not become 0). Then decide and state whether the fold should ALSO defend (a second reader eventually gets it wrong — but a fold that silently drops a non-finite value hides a producer bug; if you add a fold guard, make it observable, not silent). Test: a usage message with a missing/non-numeric input_tokens must not produce NaN totals and must not throw from the stats path.

**(a) liveUsage replace-not-merge.** `src-ui/src/hooks/orchestration/usageHandlers.ts:36-58` passes a whole `liveUsage` object literal to `updateChat`; `mergeChatUpdates` (`src-ui/src/contexts/active-chats-state.ts:832-836`) is a SHALLOW spread, so a context-only event (hasTokenUsage false) wipes held inputTokens/outputTokens/totalTokens/cacheReadTokens/cacheWriteTokens. Currently latent — the only reader (ContextPercentage via ChatDock.tsx:2197) reads contextTokens/contextWindowTokens only, and ChatLiveUsage's docblock (active-chats-state.ts:214-215) concedes it isn't wired to a token renderer yet — but it arms the moment anything renders those fields.
Fix: merge, don't replace (preserve previously-held token fields when the incoming frame carries none). Decide the layer deliberately: merging inside the handler vs teaching the store a deep-merge for this field — say why you chose it. Test: context-only frame after a token-bearing frame retains the token fields.

**(c) absent-as-zero (`|| 0`), three sites, differing severity.**
- HIGHEST: `src-server/runtime/agents/agent-hooks.ts:366-369` AND `:398-401` — persisted into message metadata via `adapter.addMessage`, feeding `applyMessageToUsageStats` (`src-server/analytics/usage-aggregator-state.ts:399-434`, itself another `|| 0` layer), so a manufactured zero compounds into LIFETIME analytics.
- REAL: `src-server/runtime/frameworks/strands-stream-events.ts:72-73` — manufactured zero enters the event stream at birth.
- LOWEST (log-only, real path gated by `if (!usage) return;` at :97): `src-server/runtime/tools/tool-execution-usage.ts:89-91`.
Fix the first two properly (absent stays absent — the fold's own rule; look at how the fold and the guarded sites express it). For the third, judge: fix if trivially the same shape, else leave with a one-line comment stating it is log-only and why that is acceptable. Do NOT convert an absent value into a zero anywhere.

**(d) cache-blind pricing + invented zero cost.** `src-server/utils/pricing.ts:27-36` `estimateCost` takes only input+output (no cache parameters at all) AND returns a hard `0` when pricing is unavailable — its own docblock admits it. `src-server/routes/chat/chat-lifecycle.ts:354` inits `estimatedCost = 0`, `:400` resets it to 0 in a catch, `:410` pushes `cost: estimatedCost` into metricsLog unconditionally — so a failed pricing lookup persists a measured-looking zero.
Fix: (i) make "unpriced" distinguishable from "free" — return undefined/absent rather than 0, and make every caller honest about absence (this is the same absent≠zero rule; note issue #463(b) is BLOCKED on this — a monitoring route wants to stop hardcoding cost:0 and needs a real absence signal from here). (ii) Make pricing cache-aware: the repo has `CacheAwareTokenComponents` available — read it and extend the signature so cached tokens can be priced when a model declares cache pricing; if pricing data has no cache rates, absence (not zero) is the honest answer. Keep the change minimal and typed; do not invent rates.

For EVERY part: unit tests that pin the honest behavior, and a fault injection per part (commit first; `git status --short | grep -v '^??' | wc -l` must print 0 before injecting; revert the guard/merge/absence, prove the new test REDS with the expected assertion text, restore byte-identical, verify with git diff). An injection that comes back GREEN is a stop signal — investigate reachability and report it, do not paper over it.

Gates before reporting: `npm run gate:for -- <all touched paths>` and run what it selects (the orchestration transfer gate may trigger on src-server/providers — if it demands a baseline, prepare one with `npm run transfer:gate -- --prepare-baseline --baseline-root ../station-worktrees/464-baseline-<basesha> --base <basesha>`, run `npm run dependencies:ci` inside it, remove any sentinel files so it is clean, then `npm run transfer:gate -- --baseline-root <root>`); `npm run lint:check`; `npm run typecheck`; and the focused tests for every file you touched. Sentinel form only (`if <cmd> > log 2>&1; then echo OK; else echo FAIL; fi`) — read the sentinel, never an exit-code summary.

Report (final message): per part — what changed (file:line), the design decision where I left one open (fold-guard question in (b), merge layer in (a), third site in (c), absence representation in (d)), test list, injection result verbatim; gate sentinels verbatim; commit SHAs; final tip SHA; anything NOT_VERIFIED. Do NOT push.
